<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Management | PawPalace Admin</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script defer src="https://unpkg.com/alpinejs@3.x.x/dist/cdn.min.js"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Quicksand:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: { sans: ['Quicksand', 'sans-serif'] },
                    colors: {
                        brand: { 50: '#fff1f2', 500: '#f43f5e' },
                        accent: { navy: '#0f172a', teal: '#2dd4bf' }
                    }
                }
            }
        }
    </script>
</head>
<body class="bg-gray-50 font-sans text-accent-navy" x-data="{ 
    showModal: false, 
    modalType: 'block', 
    selectedUser: { name: '', email: '', id: '' } 
}">

    <div class="flex min-h-screen">
        <%- include('../partials/adminSidebar') %>

        <main class="flex-1 p-8">
            <header class="flex justify-between items-center mb-8">
                <div>
                    <h1 class="text-3xl font-bold">Users</h1>
                    <p class="text-gray-500">View and manage all users</p>
                </div>
                <div class="flex items-center gap-4">
                    <div class="bg-teal-50 text-teal-600 p-2 rounded-full w-10 h-10 flex items-center justify-center font-bold">A</div>
                    <div>
                        <p class="text-sm font-bold">Admin User</p>
                        <p class="text-xs text-gray-400">Super Admin</p>
                    </div>
                </div>
            </header>

            <div class="bg-white rounded-3xl shadow-sm border border-gray-100 overflow-hidden">
                <div class="p-6 border-b border-gray-50 flex flex-wrap gap-4 items-center justify-between">
                    <div class="relative w-full max-w-md">
                        <i class="fa-solid fa-magnifying-glass absolute left-4 top-1/2 -translate-y-1/2 text-gray-400"></i>
                        <input type="text" placeholder="Search users by name or email..." class="w-full pl-12 pr-4 py-2 bg-gray-50 border border-gray-100 rounded-xl focus:outline-none focus:ring-2 focus:ring-brand-500/20 transition">
                    </div>
                    <div class="flex gap-2">
                        <input type="date" class="bg-gray-50 border border-gray-100 rounded-xl px-4 py-2 text-sm">
                        <select class="bg-gray-50 border border-gray-100 rounded-xl px-4 py-2 text-sm">
                            <option>Status</option>
                        </select>
                        <button class="bg-gray-50 border border-gray-100 rounded-xl px-4 py-2 text-sm font-bold"><i class="fa-solid fa-arrow-up-z-a mr-2"></i>Sort By</button>
                    </div>
                </div>

                <div class="overflow-x-auto">
                    <table class="w-full text-left">
                        <thead class="bg-gray-50 text-gray-400 text-xs font-bold uppercase tracking-wider">
                            <tr>
                                <th class="px-6 py-4">User ID</th>
                                <th class="px-6 py-4">Name</th>
                                <th class="px-6 py-4">Email</th>
                                <th class="px-6 py-4">Status</th>
                                <th class="px-6 py-4">Join Date</th>
                                <th class="px-6 py-4">Orders</th>
                                <th class="px-6 py-4">Actions</th>
                            </tr>
                        </thead>
                        <tbody class="divide-y divide-gray-50">
                            <% users.forEach(user => { %>
                            <tr class="hover:bg-gray-50/50 transition">
                                <td class="px-6 py-4 text-sm font-medium text-gray-400"><%= user.id %></td>
                                <td class="px-6 py-4">
                                    <div class="flex items-center gap-3">
                                        <div class="w-8 h-8 rounded-full bg-brand-50 text-brand-500 flex items-center justify-center font-bold text-xs">
                                            <%= user.name.charAt(0) %>
                                        </div>
                                        <span class="font-bold text-sm"><%= user.name %></span>
                                    </div>
                                </td>
                                <td class="px-6 py-4 text-sm text-gray-500"><%= user.email %></td>
                                <td class="px-6 py-4">
                                    <span class="px-3 py-1 rounded-full text-[10px] font-bold uppercase tracking-wide <%= user.status === 'Active' ? 'bg-green-50 text-green-600' : 'bg-red-50 text-red-600' %>">
                                        <%= user.status %>
                                    </span>
                                </td>
                                <td class="px-6 py-4 text-sm text-gray-500"><%= user.joinDate %></td>
                                <td class="px-6 py-4 text-sm font-bold"><%= user.orderCount %></td>
                                <td class="px-6 py-4">
                                    <div class="flex items-center gap-2">
                                        <% if (user.status === 'Active') { %>
                                            <button @click="showModal = true; modalType = 'block'; selectedUser = { name: '<%= user.name %>', email: '<%= user.email %>', id: '<%= user.id %>' }" 
                                                class="flex items-center gap-2 bg-brand-50 text-brand-500 px-4 py-1.5 rounded-lg text-xs font-bold hover:bg-brand-500 hover:text-white transition">
                                                <i class="fa-solid fa-lock"></i> Block
                                            </button>
                                        <% } else { %>
                                            <button @click="showModal = true; modalType = 'unblock'; selectedUser = { name: '<%= user.name %>', email: '<%= user.email %>', id: '<%= user.id %>' }"
                                                class="flex items-center gap-2 bg-teal-50 text-teal-600 px-4 py-1.5 rounded-lg text-xs font-bold hover:bg-accent-teal hover:text-white transition">
                                                <i class="fa-solid fa-lock-open"></i> Unblock
                                            </button>
                                        <% } %>
                                        <button class="p-1.5 text-gray-300 hover:text-accent-navy"><i class="fa-solid fa-pen-to-square"></i></button>
                                    </div>
                                </td>
                            </tr>
                            <% }) %>
                        </tbody>
                    </table>
                </div>

                <div class="p-6 bg-white flex justify-between items-center text-sm font-bold">
                    <span class="text-gray-400">Showing 1-8 of 156 users</span>
                    <div class="flex items-center gap-2">
                        <button class="px-4 py-2 rounded-xl border border-gray-100 text-gray-400 hover:bg-gray-50 transition"><i class="fa-solid fa-chevron-left"></i></button>
                        <button class="w-10 h-10 rounded-xl bg-brand-500 text-white">1</button>
                        <button class="w-10 h-10 rounded-xl text-gray-400 hover:bg-gray-50">2</button>
                        <button class="w-10 h-10 rounded-xl text-gray-400 hover:bg-gray-50">3</button>
                        <span class="text-gray-300">...</span>
                        <button class="w-10 h-10 rounded-xl text-gray-400 hover:bg-gray-50">20</button>
                        <button class="px-4 py-2 rounded-xl border border-gray-100 text-gray-400 hover:bg-gray-50 transition"><i class="fa-solid fa-chevron-right"></i></button>
                    </div>
                </div>
            </div>
        </main>
    </div>

    <div x-show="showModal" 
         x-transition:enter="transition ease-out duration-300"
         x-transition:enter-start="opacity-0"
         x-transition:enter-end="opacity-100"
         x-transition:leave="transition ease-in duration-200"
         x-transition:leave-start="opacity-100"
         x-transition:leave-end="opacity-0"
         class="fixed inset-0 z-[60] flex items-center justify-center p-4 bg-accent-navy/40 backdrop-blur-sm"
         style="display: none;">
        
        <div @click.away="showModal = false" 
             class="bg-white rounded-[2.5rem] p-10 max-w-md w-full shadow-2xl text-center transform transition-all border-4 border-white/50">
            
            <div class="w-20 h-20 bg-red-50 text-red-500 rounded-full flex items-center justify-center mx-auto mb-6 text-3xl">
                <i :class="modalType === 'block' ? 'fa-solid fa-triangle-exclamation' : 'fa-solid fa-shield-cat'"></i>
            </div>

            <h3 class="text-2xl font-bold mb-2" x-text="modalType === 'block' ? 'Block this user?' : 'Unblock this user?'"></h3>
            <p class="text-gray-500 text-sm mb-8 leading-relaxed">
                Are you sure you want to <span x-text="modalType"></span> <strong x-text="selectedUser.name"></strong>? 
                They will <span x-text="modalType === 'block' ? 'no longer' : 'now'"></span> be able to access their account or make purchases.
            </p>

            <div class="bg-red-50/50 rounded-3xl p-4 flex items-center gap-4 mb-8 text-left border border-red-50">
                <div class="w-12 h-12 bg-white rounded-2xl flex items-center justify-center font-bold text-red-500 shadow-sm" x-text="selectedUser.name.charAt(0)"></div>
                <div>
                    <p class="font-bold text-sm" x-text="selectedUser.name"></p>
                    <p class="text-xs text-gray-400" x-text="selectedUser.email"></p>
                    <p class="text-[10px] text-gray-300 font-bold" x-text="selectedUser.id"></p>
                </div>
            </div>

            <div class="flex flex-col gap-3">
                <form :action="`/admin/users/${selectedUser.id}/${modalType === 'block' ? 'block' : 'unblock'}`" method="POST">
                    <button type="submit" class="w-full py-4 bg-brand-500 text-white rounded-2xl font-bold shadow-lg shadow-brand-500/20 hover:-translate-y-1 transition-all">
                        Confirm <span x-text="modalType.charAt(0).toUpperCase() + modalType.slice(1)"></span>
                    </button>
                </form>
                <button @click="showModal = false" class="w-full py-4 bg-white border border-gray-100 text-gray-400 rounded-2xl font-bold hover:bg-gray-50 transition">
                    Cancel
                </button>
            </div>
        </div>
    </div>

</body>
</html>