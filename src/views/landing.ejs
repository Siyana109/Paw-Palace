<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> | Playful & Premium Pet Care</title>
    
    <script src="https://cdn.tailwindcss.com"></script>
    
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    
    <link href="https://fonts.googleapis.com/css2?family=Quicksand:wght@300;400;500;600;700&display=swap" rel="stylesheet">

    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: { sans: ['Quicksand', 'sans-serif'] },
                    colors: {
                        brand: { 50: '#fff1f2', 100: '#ffe4e6', 200: '#fecdd3', 500: '#f43f5e', 600: '#e11d48' },
                        accent: { teal: '#2dd4bf', tealDark: '#0d9488', navy: '#0f172a', cream: '#fffdf5' }
                    },
                    animation: {
                        'float': 'float 6s ease-in-out infinite',
                        'blob': 'blob 10s infinite',
                    },
                    keyframes: {
                        float: { '0%, 100%': { transform: 'translateY(0)' }, '50%': { transform: 'translateY(-10px)' } },
                        blob: {
                            '0%': { transform: 'translate(0px, 0px) scale(1)' },
                            '33%': { transform: 'translate(30px, -50px) scale(1.1)' },
                            '66%': { transform: 'translate(-20px, 20px) scale(0.9)' },
                            '100%': { transform: 'translate(0px, 0px) scale(1)' },
                        }
                    }
                }
            }
        }
    </script>

    <style>
        html { scroll-behavior: smooth; }
        .glass-nav { background: rgba(255, 255, 255, 0.95); backdrop-filter: blur(12px); border-bottom: 2px solid rgba(244, 63, 94, 0.1); }
        .hero-pattern {
            background-color: #fff1f2;
            background-image: radial-gradient(#f43f5e 1px, transparent 1px);
            background-size: 20px 20px;
            opacity: 0.3;
        }
    </style>
</head>
<body class="font-sans text-accent-navy antialiased bg-accent-cream selection:bg-accent-teal selection:text-white">

    <% if (typeof announcement !== 'undefined') { %>
        <div class="bg-accent-navy text-white text-xs font-bold text-center py-2.5 tracking-wide">
            <%= announcement %>
        </div>
    <% } %>

    <nav class="fixed w-full z-50 glass-nav transition-all duration-300" id="navbar">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex justify-between items-center h-20">
                <a href="/" class="flex items-center gap-2 group">
                    <div class="bg-brand-500 text-white p-2 rounded-2xl shadow-[4px_4px_0px_0px_rgba(13,148,136,1)] transform group-hover:translate-x-1 group-hover:translate-y-1 group-hover:shadow-none transition-all duration-200">
                        <i class="fa-solid fa-heart text-lg"></i>
                    </div>
                    <span class="font-extrabold text-2xl tracking-tight text-accent-navy">Paw<span class="text-brand-500">Palace</span></span>
                </a>

                <div class="hidden md:flex space-x-8">
                    <a href="/" class="text-gray-600 hover:text-brand-500 font-bold transition">Home</a>
                    <a href="/profile" class="text-gray-600 hover:text-brand-500 font-bold transition">Profile</a>
                </div>

                <div class="flex items-center gap-4">
                    <button class="text-gray-400 hover:text-brand-500 transition"><i class="fa-solid fa-magnifying-glass text-lg"></i></button>
                    
                    <% if (typeof user !== 'undefined') { %>
                        <a href="/profile" class="p-2.5 bg-brand-50 text-brand-500 rounded-xl hover:bg-brand-500 hover:text-white transition">
                            <i class="fa-solid fa-user"></i>
                        </a>
                    <% }
                     else { %>
                        <a href="/login" class="text-sm font-bold text-accent-navy hover:text-brand-500 transition">Login</a>
                    <% } %>

                    <button class="relative group">
                        <div class="p-2.5 bg-accent-teal/10 hover:bg-accent-teal/20 text-accent-tealDark rounded-xl transition">
                            <i class="fa-solid fa-cart-shopping text-lg"></i>
                        </div>
                        <span class="absolute -top-1 -right-1 bg-brand-500 text-white text-[10px] font-bold w-5 h-5 flex items-center justify-center rounded-full border-2 border-white">
                            <%= typeof cartCount !== 'undefined' ? cartCount : '0' %>
                        </span>
                    </button>
                </div>
            </div>
        </div>
    </nav>

    <section id="home" class="relative pt-36 pb-24 overflow-hidden bg-brand-50">
        <div class="absolute inset-0 hero-pattern z-0"></div>
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 relative z-10">
            <div class="flex flex-col lg:flex-row items-center gap-12">
                <div class="lg:w-1/2 text-center lg:text-left">
                    <div class="inline-flex items-center gap-2 px-4 py-1.5 rounded-full bg-white border-2 border-brand-200 shadow-sm mb-8 animate-float">
                        <span class="text-lg">🐾</span>
                        <span class="text-xs font-bold text-brand-500 uppercase tracking-wide">Where Pets Are Family</span>
                    </div>
                    <h1 class="text-5xl lg:text-7xl font-extrabold text-accent-navy leading-[1.1] mb-6">
                        Unleash The <br>
                        <span class="text-transparent bg-clip-text bg-gradient-to-r from-brand-500 to-accent-teal">Joy & Love.</span>
                    </h1>
                    <p class="text-xl text-gray-500 mb-10 max-w-lg mx-auto lg:mx-0 font-medium leading-relaxed">
                        Colorful toys, yummy organic treats, and gentle grooming. Everything you need to make them wag, purr, and chirp!
                    </p>
                    <div class="flex flex-col sm:flex-row gap-4 justify-center lg:justify-start">
                        <a href="/home" class="px-8 py-4 bg-brand-500 text-white rounded-2xl font-bold shadow-[4px_4px_0px_0px_rgba(13,148,136,1)] hover:shadow-none hover:translate-x-[2px] hover:translate-y-[2px] transition-all border-2 border-brand-600">
                            Start Shopping
                        </a>
                    </div>
                </div>

                <div class="lg:w-1/2 relative">
                    <div class="relative z-10 rounded-[2.5rem] overflow-hidden border-[8px] border-white shadow-2xl transform hover:rotate-2 transition duration-500">
                        <img src="https://images.unsplash.com/photo-1517849845537-4d257902454a?auto=format&fit=crop&w=800&q=80" alt="Happy Dog" class="w-full h-[550px] object-cover">
                    </div>
                </div>
            </div>
        </div>
    </section>

    <section id="products" class="py-24 bg-accent-cream">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <h2 class="text-3xl font-bold text-accent-navy mb-12 text-center">Popular Picks</h2>
            
            <div id="product-grid" class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6">
                <% if (products && products.length > 0) { %>
                    <% products.forEach(product => { %>
                        <div class="bg-white rounded-3xl p-4 shadow-sm hover:shadow-xl transition-all group border border-gray-100 flex flex-col hover:-translate-y-1">
                            <div class="relative h-56 rounded-2xl overflow-hidden mb-4 bg-brand-50">
                                <img src="<%= product.image %>" alt="<%= product.title %>" class="w-full h-full object-cover group-hover:scale-105 transition duration-700">
                                <% if (product.badge) { %>
                                    <span class="absolute top-3 left-3 bg-white/95 backdrop-blur text-[10px] font-bold px-3 py-1.5 rounded-full text-brand-500 shadow-sm uppercase tracking-wide flex items-center gap-1">
                                        <i class="fa-solid fa-face-smile text-accent-teal"></i> <%= product.badge %>
                                    </span>
                                <% } %>
                                <button class="absolute bottom-3 right-3 w-10 h-10 bg-white rounded-xl shadow-lg flex items-center justify-center text-accent-tealDark hover:bg-brand-500 hover:text-white transition transform translate-y-12 opacity-0 group-hover:translate-y-0 group-hover:opacity-100 duration-300">
                                    <i class="fa-solid fa-plus"></i>
                                </button>
                            </div>
                            <div class="flex-1 px-1">
                                <div class="text-[10px] text-accent-tealDark font-extrabold uppercase mb-1.5 tracking-wider"><%= product.category %></div>
                                <h3 class="font-bold text-accent-navy text-lg leading-tight mb-2 group-hover:text-brand-500 transition"><%= product.title %></h3>
                                <span class="text-xl font-extrabold text-accent-navy">$<%= product.price.toFixed(2) %></span>
                            </div>
                        </div>
                    <% }); %>
                <% } else { %>
                    <p class="col-span-full text-center text-gray-400">No products found!</p>
                <% } %>
            </div>
        </div>
    </section>

    <footer class="bg-white text-accent-navy pt-20 pb-10 border-t border-brand-100">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
            <div class="flex justify-center items-center gap-2 mb-6">
                <div class="bg-brand-500 text-white w-8 h-8 rounded-lg flex items-center justify-center">
                    <i class="fa-solid fa-heart text-sm"></i>
                </div>
                <span class="font-extrabold text-2xl tracking-tight">Paw<span class="text-brand-500">Palace</span></span>
            </div>
            <p class="text-sm text-gray-400">&copy; 2025 PawPalace. Made with love for furry friends.</p>
        </div>
    </footer>

    <script>
        window.addEventListener('scroll', () => {
            const nav = document.getElementById('navbar');
            window.scrollY > 20 ? nav.classList.add('shadow-md') : nav.classList.remove('shadow-md');
        });
    </script>
</body>
</html>