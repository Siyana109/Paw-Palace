<nav class="bg-white/80 backdrop-blur-md sticky top-0 z-50 border-b border-gray-100">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between items-center h-20">

            <a href="/" class="flex items-center gap-2 group cursor-pointer">
                <div
                    class="bg-brand-500 text-white p-2 rounded-xl shadow-lg shadow-brand-200 group-hover:scale-110 transition duration-300">
                    <i class="fa-solid fa-paw text-xl"></i>
                </div>
                <span class="font-extrabold text-2xl tracking-tight text-accent-navy">
                    Paw<span class="text-brand-500">Palace</span>
                </span>
            </a>

            <div class="hidden md:flex items-center gap-8">
                <!-- <a href="/" class="text-gray-500 hover:text-accent-navy font-bold transition">Home</a>
                <a href="/shop" class="text-brand-500 font-bold border-b-2 border-brand-500 pb-1">Shop All</a> -->
                <a href="/"
                    class="<%= currentPath === '/' ? 'text-brand-500 font-bold border-b-2 border-brand-500 pb-1' : 'text-gray-500 hover:text-accent-navy font-bold transition' %>">
                    Home
                </a>

                <a href="/home"
                    class="<%= currentPath.startsWith('/home') ? 'text-brand-500 font-bold border-b-2 border-brand-500 pb-1' : 'text-gray-500 hover:text-accent-navy font-bold transition' %>">
                    Shop All
                </a>

            </div>

            <div class="flex items-center gap-6 text-accent-navy">
                <button class="hover:text-brand-500 transition relative group">
                    <i class="fa-regular fa-heart text-2xl"></i>
                    <span
                        class="absolute -top-1 -right-1 bg-accent-navy text-white text-[10px] font-bold h-4 w-4 rounded-full flex items-center justify-center border-2 border-white group-hover:bg-brand-500">
                        <%= wishlistCount %>
                    </span>
                </button>

                <button class="hover:text-brand-500 transition relative group">
                    <i class="fa-solid fa-cart-shopping text-2xl"></i>
                    <span
                        class="absolute -top-1 -right-1 bg-brand-500 text-white text-[10px] font-bold h-4 w-4 rounded-full flex items-center justify-center border-2 border-white"><span>
                            <%= cartCount %>
                        </span></span>
                </button>

                <!-- <div
                    class="h-10 w-10 bg-teal-50 rounded-xl flex items-center justify-center text-accent-teal hover:bg-accent-teal hover:text-white transition cursor-pointer relative">
                    <i class="fa-solid fa-user"></i>
                    <span
                        class="absolute -top-1 -right-1 bg-brand-500 h-2.5 w-2.5 rounded-full border-2 border-white"></span>
                </div> -->

                <a href="<%= user ? '/profile' : '/login' %>" class="h-10 w-10 rounded-xl flex items-center justify-center transition relative
                   <%= currentPath.startsWith('/profile')  ? 'bg-accent-teal text-white'  : 'bg-teal-50 text-accent-teal hover:bg-accent-teal hover:text-white' %>">
                    <i class="fa-solid fa-user"></i>

                    <% if (user) { %>
                        <span
                            class="absolute -top-1 -right-1 bg-brand-500 h-2.5 w-2.5 rounded-full border-2 border-white">
                        </span>
                        <% } %>
                </a>



            </div>
        </div>
    </div>
</nav>